
#ifndef _BENCHMARKS_H_
#define _BENCHMARKS_H_

// Runs the compress benchmark.
int main_compress(void);

// Runs the convolution_7x7 benchmark.
int main_convolution_7x7(void);

// Runs the median benchmark.
int main_median(void);

// Runs the bcnt benchmark.
int main_bcnt(void);

#endif
